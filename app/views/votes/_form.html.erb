<%= form_for([@post,@post.votes.build]) do |f| %>
  <%= f.number_field :vote ,value: 1, type: :hidden %>
  <%= f.submit :Yes %>
<%end%>
<%= form_for([@post,@post.votes.build]) do |f| %>
  <%= f.number_field :vote ,value: -1, type: :hidden %>
  <%= f.submit :No %>
<%end%>
